import React from 'react';
import ContentManagerDashboard from './ContentManagerDashboard';

export const ContentManagerDashboardWrapper: React.FC = () => {
  // Pure structural wrapper for future data hooks or pipeline settings
  return <ContentManagerDashboard />;
};

export default ContentManagerDashboardWrapper;
