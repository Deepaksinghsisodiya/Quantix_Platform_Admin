export enum UserStatus {
    Active = 0,
    Inactive = 1,
    OnLeave = 2,
    Terminated = 3
}

export enum AttendanceStatus {
    Present = 0,
    Absent = 1,
    Late = 2,
    HalfDay = 3,
    OnLeave = 4
}
